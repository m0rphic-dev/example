/**
 * m0rphic React Example - Entry Point
 *
 * This example demonstrates how to integrate the m0rphic SDK
 * into a React application for AI-powered UI optimization.
 */

import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
